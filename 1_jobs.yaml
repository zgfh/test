apiVersion: batch/v1
kind: Job
metadata:
  name: jobs1
spec:
  template:
    metadata:
      name: jobs1
    spec:
      containers:
      - name: pi
        image: perl
        command: ["perl",  "-Mbignum=bpi", "-wle", "print bpi(2000)"]
      restartPolicy: Never
